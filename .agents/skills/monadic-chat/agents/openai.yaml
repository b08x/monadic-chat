interface:
  display_name: "Monadic Chat"
  short_description: "Repo-specific patterns and workflows for monadic-chat"
  default_prompt: "Use the monadic-chat repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true